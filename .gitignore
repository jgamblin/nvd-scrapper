# Scraper output (never committed — uploaded to R2)
nvd.json
nvd.jsonl
metadata.json

# Watchdog high-water marks (restored from the Actions cache in CI)
.mirror-state.json

# Logs
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor
.vscode/
.idea/
.DS_Store

# Local secrets (never committed — these live in GitHub repo secrets)
.env
.env.local

# Claude Code local state
.claude/
